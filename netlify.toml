[build]
  command = "NEXT_PUBLIC_SITE_URL=https://elevatedtechsolutionsllc.com npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[dev]
  command = "npm run dev"
  targetPort = 3000
